# 🚂 Деплой Nova Syla API на Railway.app

## ✨ Переваги Railway:

- ✅ Швидший деплой (1-2 хвилини)
- ✅ Автоматичний HTTPS
- ✅ Безкоштовний план ($5 кредит/місяць)
- ✅ Не засипає (на відміну від Render)
- ✅ Простіша конфігурація

## 🚀 Крок 1: Підготовка коду

✅ Всі файли готові:
- `railway.json` - конфігурація Railway
- `.node-version` - Node.js версія
- `package.json` - скрипти збірки

## 🚀 Крок 2: Створення проекту на Railway

### Варіант A: Через веб-інтерфейс (рекомендовано)

1. **Перейдіть на https://railway.app**
2. **Sign up with GitHub** (увійдіть через GitHub)
3. **New Project → Deploy from GitHub repo**
4. Оберіть репозиторій (або створіть новий)
5. Railway автоматично виявить Node.js проект

### Варіант B: Через Railway CLI

```bash
# Встановлення Railway CLI
npm install -g @railway/cli

# Логін
railway login

# Ініціалізація проекту
cd backend
railway init

# Деплой
railway up
```

## 🔧 Крок 3: Налаштування змінних оточення

У Railway Dashboard → Variables додайте:

```env
NODE_ENV=production
PORT=3001
DB_SERVER=10.131.10.25
DB_DATABASE=AZIT
DB_USER=zeus
DB_PASSWORD=zeus
DB_PORT=1433
BINOTEL_KEY=035963-ac29f32
BINOTEL_SECRET=b3aa55-bf46d3-b0e6a6-99dc8a-8f8a984c
BINOTEL_COMPANY_ID=57612
JWT_SECRET=nova_syla_loyalty_secret_key_2024
```

⚠️ **ВАЖЛИВО**: 
- `DB_SERVER=10.131.10.25` - локальна адреса
- Railway не зможе підключитись до локальної БД
- Потрібна хмарна БД або VPN

## 📦 Крок 4: Альтернатива - Railway PostgreSQL

Railway пропонує безкоштовну PostgreSQL БД:

1. В проекті: **New → Database → PostgreSQL**
2. Railway автоматично додасть `DATABASE_URL`
3. Потрібно адаптувати код під PostgreSQL

## 🔗 Крок 5: Отримання URL

Після деплою Railway надасть URL:
```
https://nova-syla-api-production.up.railway.app
```

## 📱 Крок 6: Оновлення Flutter

**lib/config/api_config.dart:**
```dart
static const String baseUrl = 'https://YOUR-APP.up.railway.app/api/v2';
```

Перезбілдіть веб:
```bash
flutter build web --release
surge build/web nova-syla-loyalty.surge.sh
```

## 🎯 URLs після деплою

- **Backend API**: https://YOUR-APP.up.railway.app
- **Frontend**: https://nova-syla-loyalty.surge.sh
- **Health**: https://YOUR-APP.up.railway.app/health
- **API Docs**: https://YOUR-APP.up.railway.app/api/docs

## 💰 Pricing

- **Free**: $5 кредит/місяць (~100-150 годин роботи)
- **Hobby**: $5/міс - необмежено
- Без кредитної картки для старту

## 🔍 Моніторинг

Railway Dashboard показує:
- Логи в реальному часі
- Metrics (CPU, RAM, Network)
- Deploy history
- Environment variables

## 🚨 Проблема з локальною БД

**Рішення 1: Azure SQL Database**
- Створіть БД на Azure
- Оновіть `DB_SERVER` на публічну адресу

**Рішення 2: Railway PostgreSQL**
- Додайте PostgreSQL в проект
- Міграція з MSSQL на PostgreSQL
- Оновіть код (замість `mssql` → `pg`)

**Рішення 3: Cloudflare Tunnel**
- Створіть тунель до локальної БД
- Безпечний доступ через Cloudflare

## 📝 Команди Railway CLI

```bash
# Перегляд логів
railway logs

# Відкрити проект в браузері
railway open

# Перезапуск
railway restart

# Видалення
railway delete
```

## ✅ Checklist

- [ ] Створено GitHub репозиторій
- [ ] Запушено код
- [ ] Створено проект на Railway
- [ ] Додано змінні оточення
- [ ] Деплой успішний
- [ ] API доступний
- [ ] Оновлено URL у Flutter
- [ ] Перезбілдено веб
- [ ] Передеплоєно на Surge
