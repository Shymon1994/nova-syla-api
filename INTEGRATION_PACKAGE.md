# Пакет інтеграції Nova Syla з АБ Офіс

## Зміст пакету

Цей пакет містить все необхідне для інтеграції системи лояльності Nova Syla з програмою АБ Офіс.

## 📁 Файли

### 1. SQL Скрипти

**`database_setup.sql`** - Основний скрипт встановлення
- Створює таблицю `QRCodes`
- Створює 5 stored procedures для роботи з QR кодами
- Додає індекси для оптимізації
- Включає тестові приклади

### 2. Документація

**`SQL_API_DOCUMENTATION.md`** - Технічна документація API
- Детальний опис усіх endpoints
- Приклади запитів та відповідей
- Структура таблиць та stored procedures
- cURL приклади для тестування

**`README_SQL_INTEGRATION.md`** - Інструкція з інтеграції
- Покроковий гайд встановлення
- Сценарії використання в АБ Офіс
- Приклади коду для інтеграції
- Troubleshooting та FAQ
- Чеклист інтеграції

### 3. Код

**`src/routes/qr.ts`** - Node.js API для QR кодів
- POST /api/qr/generate - генерація QR
- GET /api/qr/validate/:token - перевірка валідності
- POST /api/qr/use - використання QR в касі
- GET /api/qr/history/:phone - історія QR кодів

## 🚀 Швидкий старт (для SQL програміста)

### Крок 1: Встановлення бази даних

```bash
# Через sqlcmd
sqlcmd -S 10.131.10.25 -d AZIT -U zeus -P zeus -i database_setup.sql

# Або через SSMS
1. Відкрийте database_setup.sql
2. Підключіться до 10.131.10.25
3. Виберіть базу AZIT
4. Виконайте (F5)
```

### Крок 2: Тестування

```sql
-- Тест 1: Створення QR
EXEC zeus_CreateQR
    @QRToken = 'TEST_TOKEN_123',
    @PhoneNum = '380685552629',
    @ClientName = 'Тест',
    @Balance = 1000,
    @ValidUntil = DATEADD(MINUTE, 5, GETDATE()),
    @IsUsed = 0;

-- Тест 2: Перевірка валідності
EXEC zeus_ValidateQR @QRToken = 'TEST_TOKEN_123';

-- Тест 3: Використання QR
EXEC zeus_UseQR 
    @QRToken = 'TEST_TOKEN_123',
    @StoreId = 'STORE_001',
    @CashierId = 'CASHIER_123',
    @TransactionAmount = 1500.50;

-- Тест 4: Історія
EXEC zeus_GetQRHistory @PhoneNum = '380685552629';
```

### Крок 3: Інтеграція з АБ Офіс

Додайте в АБ Офіс функціонал сканування QR та виклику stored procedures:

```sql
-- 1. Сканування QR коду касиром
DECLARE @ScannedToken NVARCHAR(100) = 'token_from_scanner';

-- 2. Перевірка валідності
DECLARE @IsValid BIT;
EXEC zeus_ValidateQR @QRToken = @ScannedToken;
-- Отримайте IsValid з результату

-- 3. Якщо valid, виконайте транзакцію в АБ Офіс

-- 4. Після успішної транзакції, позначте QR як використаний
EXEC zeus_UseQR 
    @QRToken = @ScannedToken,
    @StoreId = 'ваш_store_id',
    @CashierId = 'ваш_cashier_id',
    @TransactionAmount = 'сума_покупки';
```

## 📊 Архітектура

```
┌─────────────────┐
│ Мобільний додаток│
│   Nova Syla     │
└────────┬────────┘
         │ QR код генерується
         ↓
┌─────────────────┐
│   Node.js API   │
│  (localhost:3001)│
└────────┬────────┘
         │ Зберігає в БД
         ↓
┌─────────────────┐
│  SQL Server     │
│  AZIT Database  │
│  QRCodes Table  │
└────────┬────────┘
         │ Читає дані
         ↓
┌─────────────────┐
│    АБ Офіс      │
│  (Каса/Продаж)  │
└─────────────────┘
```

## 🔑 Ключові концепції

### QR Token
- 64-символьний hex string
- Унікальний для кожного QR коду
- Генерується через crypto.randomBytes(32)
- Приклад: `a3b5c7d9e1f2g3h4i5j6k7l8m9n0o1p2...`

### Lifecycle QR коду
1. **Створення** - додаток генерує QR (5 хв життя)
2. **Валідація** - касир сканує і перевіряє
3. **Використання** - після транзакції QR позначається як використаний
4. **Історія** - клієнт може переглянути всі QR в додатку

### Безпека
- ✅ Термін дії 5 хвилин
- ✅ Одноразове використання
- ✅ Унікальні непередбачувані токени
- ✅ Зберігання інформації про транзакцію

## 📋 Stored Procedures

### 1. zeus_CreateQR
**Призначення**: Створення нового QR коду  
**Параметри**: QRToken, PhoneNum, ClientName, Balance, ValidUntil, IsUsed  
**Використання**: Викликається Node.js API при генерації QR в додатку

### 2. zeus_ValidateQR
**Призначення**: Перевірка валідності QR коду  
**Параметри**: QRToken  
**Повертає**: IsValid (1 = можна використовувати, 0 = застарів/використаний)  
**Використання**: Викликається в АБ Офіс після сканування QR

### 3. zeus_UseQR
**Призначення**: Позначити QR як використаний після транзакції  
**Параметри**: QRToken, StoreId, CashierId, TransactionAmount  
**Повертає**: Success (1/0) + інформація про клієнта  
**Використання**: Викликається в АБ Офіс після успішної оплати

### 4. zeus_GetQRHistory
**Призначення**: Отримання історії всіх QR кодів клієнта  
**Параметри**: PhoneNum  
**Повертає**: Список всіх QR з статусами  
**Використання**: Викликається мобільним додатком для показу історії

### 5. zeus_CleanupExpiredQR
**Призначення**: Видалення старих QR кодів  
**Параметри**: DaysToKeep (за замовчуванням 30)  
**Використання**: Запускається через SQL Agent Job щоденно

## 🔧 Налаштування

### Підключення до API

**Base URL**: `http://localhost:3001/api`

**Endpoints**:
- `POST /api/qr/generate` - генерація QR
- `GET /api/qr/validate/:token` - перевірка
- `POST /api/qr/use` - використання
- `GET /api/qr/history/:phone` - історія

### Підключення до SQL

```
Server: 10.131.10.25
Database: AZIT
User: zeus
Password: zeus
```

## ✅ Чеклист для SQL програміста

- [ ] Прочитав усю документацію
- [ ] Виконав database_setup.sql
- [ ] Перевірив створення таблиці QRCodes
- [ ] Перевірив усі 5 stored procedures
- [ ] Протестував створення QR
- [ ] Протестував валідацію QR
- [ ] Протестував використання QR
- [ ] Розумію архітектуру системи
- [ ] Знаю як інтегрувати з АБ Офіс
- [ ] Налаштував SQL Agent Job для очистки
- [ ] Готовий до production deployment

## 📞 Підтримка

При виникненні питань:
1. Перегляньте `SQL_API_DOCUMENTATION.md` для деталей API
2. Перегляньте `README_SQL_INTEGRATION.md` для прикладів інтеграції
3. Перевірте розділ Troubleshooting в документації

## 📝 Примітки

- Всі stored procedures мають обробку помилок
- Використовуються транзакції де потрібно
- Додані індекси для оптимізації
- Структура таблиці розширювана для майбутніх потреб

---

**Версія**: 1.0.0  
**Дата**: 20 грудня 2025  
**Статус**: Готово до впровадження
